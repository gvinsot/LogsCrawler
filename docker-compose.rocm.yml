# AMD ROCm GPU Support Override for Ollama
# Usage: docker-compose -f docker-compose.yml -f docker-compose.rocm.yml up -d

version: '3.8'

services:
  ollama:
    image: ollama/ollama:rocm
    devices:
      - /dev/kfd
      - /dev/dri
    group_add:
      - video
      - render
